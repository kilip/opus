// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kilip/opus/api/ent/wachat"
	"github.com/kilip/opus/api/ent/wasession"
)

// WaChat is the model entity for the WaChat schema.
type WaChat struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Jid holds the value of the "jid" field.
	Jid string `json:"jid,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// UnreadCount holds the value of the "unread_count" field.
	UnreadCount int `json:"unread_count,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WaChatQuery when eager-loading is set.
	Edges            WaChatEdges `json:"edges"`
	wa_session_chats *string
	selectValues     sql.SelectValues
}

// WaChatEdges holds the relations/edges for other nodes in the graph.
type WaChatEdges struct {
	// WaSession holds the value of the wa_session edge.
	WaSession *WaSession `json:"wa_session,omitempty"`
	// Messages holds the value of the messages edge.
	Messages []*WaMessage `json:"messages,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// WaSessionOrErr returns the WaSession value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WaChatEdges) WaSessionOrErr() (*WaSession, error) {
	if e.WaSession != nil {
		return e.WaSession, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: wasession.Label}
	}
	return nil, &NotLoadedError{edge: "wa_session"}
}

// MessagesOrErr returns the Messages value or an error if the edge
// was not loaded in eager-loading.
func (e WaChatEdges) MessagesOrErr() ([]*WaMessage, error) {
	if e.loadedTypes[1] {
		return e.Messages, nil
	}
	return nil, &NotLoadedError{edge: "messages"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WaChat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case wachat.FieldID, wachat.FieldUnreadCount:
			values[i] = new(sql.NullInt64)
		case wachat.FieldJid, wachat.FieldName:
			values[i] = new(sql.NullString)
		case wachat.ForeignKeys[0]: // wa_session_chats
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WaChat fields.
func (_m *WaChat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case wachat.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case wachat.FieldJid:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field jid", values[i])
			} else if value.Valid {
				_m.Jid = value.String
			}
		case wachat.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case wachat.FieldUnreadCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field unread_count", values[i])
			} else if value.Valid {
				_m.UnreadCount = int(value.Int64)
			}
		case wachat.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wa_session_chats", values[i])
			} else if value.Valid {
				_m.wa_session_chats = new(string)
				*_m.wa_session_chats = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WaChat.
// This includes values selected through modifiers, order, etc.
func (_m *WaChat) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryWaSession queries the "wa_session" edge of the WaChat entity.
func (_m *WaChat) QueryWaSession() *WaSessionQuery {
	return NewWaChatClient(_m.config).QueryWaSession(_m)
}

// QueryMessages queries the "messages" edge of the WaChat entity.
func (_m *WaChat) QueryMessages() *WaMessageQuery {
	return NewWaChatClient(_m.config).QueryMessages(_m)
}

// Update returns a builder for updating this WaChat.
// Note that you need to call WaChat.Unwrap() before calling this method if this WaChat
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *WaChat) Update() *WaChatUpdateOne {
	return NewWaChatClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the WaChat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *WaChat) Unwrap() *WaChat {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: WaChat is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *WaChat) String() string {
	var builder strings.Builder
	builder.WriteString("WaChat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("jid=")
	builder.WriteString(_m.Jid)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("unread_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.UnreadCount))
	builder.WriteByte(')')
	return builder.String()
}

// WaChats is a parsable slice of WaChat.
type WaChats []*WaChat
